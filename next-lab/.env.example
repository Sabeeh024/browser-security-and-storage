BACKEND_URL=http://localhost:8787
# server-to-server credential — never shipped to the browser
BACKEND_API_KEY=dev-backend-key

# inlined into the client bundle at build time. public.
NEXT_PUBLIC_APP_NAME=next-lab
